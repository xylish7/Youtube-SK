interface RoutesNames {
  HOME: string;
  SETTINGS: string;
  DOWNLOAD: string;
  CONVERT: string;
  CUT: string;
  PLAYER: string;
}

const routes: RoutesNames = {
  HOME: '/',
  SETTINGS: '/settings',
  DOWNLOAD: '/download',
  CONVERT: '/convert',
  CUT: '/cut',
  PLAYER: '/player'
};

export default routes;
